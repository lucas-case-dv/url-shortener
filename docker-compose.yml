services:
  traefik:
    image: traefik:v3.6.6
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--entrypoints.web.address=:80"
    ports:
      - "80:80"
      - "8081:8081"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"

  db:
    image: postgres:latest
    environment:
      - POSTGRES_USER=$POSTGRES_USER
      - POSTGRES_PASSWORD=$POSTGRES_PASSWORD
      - POSTGRES_DB=$POSTGRES_DB
    volumes:
      - pg_data:/var/lib/postgresql
    ports:
      - $POSTGRES_LOCAL_PORT:$POSTGRES_DOCKER_PORT
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}" ]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    depends_on:
      db:
        condition: service_healthy
    build:
      context: ./
      dockerfile: Dockerfile
    working_dir: /usr/src/url-shortener
    container_name: url-shortener
    restart: on-failure
    env_file: ./.env
    ports:
      - $SPRING_LOCAL_PORT:$SPRING_DOCKER_PORT
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:$POSTGRES_DOCKER_PORT/$POSTGRES_DB
      - SPRING_DATASOURCE_USERNAME=$POSTGRES_USER
      - SPRING_DATASOURCE_PASSWORD=$POSTGRES_PASSWORD
    volumes:
      - .m2:/root/.m2
    stdin_open: true
    tty: true
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api-data.rule=Host(`url.localhost`) && PathPrefix(`/api`)"
      - "traefik.http.routers.api-data.priority=100"
      - "traefik.http.routers.stats-view.rule=Host(`url.localhost`) && PathRegexp(`^/[a-zA-Z0-9_-]+/stats$|/stats`)"
      - "traefik.http.routers.stats-view.priority=80"
      - "traefik.http.middlewares.stats-replace.replacepath.path=/stats.html"
      - "traefik.http.routers.stats-view.middlewares=stats-replace"
      - "traefik.http.routers.redirect.rule=Host(`url.localhost`) && PathPrefix(`/`)"
      - "traefik.http.routers.redirect.priority=50"

volumes:
  pg_data:
  maven_cache:
